spring:
  config:
    import: "optional:configserver:http://svc-msa-config-server:8081/"
  application:
    name: website-service
  profiles:
    active: dev
  cloud:
    compatibility-verifier:
      enabled: false
  mvc:
    servlet:
      path: '/lottery-service'